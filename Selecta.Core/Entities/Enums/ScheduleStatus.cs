namespace Selecta.Core.Entities.Enums;

/// <summary>Corresponde ao enum "StatusAgendamento" original — valores numéricos preservados.</summary>
public enum ScheduleStatus
{
    Pending = 0,
    Present = 1,
    Absent = 2,
    Cancelled = 3,
}
