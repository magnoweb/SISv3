namespace Selecta.Core.Entities.Enums;

/// <summary>Corresponde ao enum "ServicosBase" original — valores numéricos preservados.</summary>
public enum ServiceOrigin
{
    Recruitment = 1,
    Selection = 2,
    General = 3,
}
