namespace Selecta.Core.Entities.Enums;

/// <summary>
/// Corresponde ao enum "Genero" original. Os valores numéricos têm de se
/// manter EXATAMENTE iguais (Female=1, Male=2) — é assim que já estão
/// gravados na coluna "Genero" da tabela "Candidatos".
/// </summary>
public enum Gender
{
    Female = 1,
    Male = 2,
}
