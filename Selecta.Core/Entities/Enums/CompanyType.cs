namespace Selecta.Core.Entities.Enums;

/// <summary>Corresponde ao enum "TipoEmpresa" original — valores numéricos preservados.</summary>
public enum CompanyType
{
    Both = 0,
    Client = 1,
    Partner = 2,
}
