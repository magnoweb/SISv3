namespace Selecta.Core.Entities.Enums;

/// <summary>Corresponde ao enum "GrupoCompetencia" original — valores numéricos preservados.</summary>
public enum CompetencyGroup
{
    Undefined = 0,
    Behavioral = 1,
    Skill = 2,
}
