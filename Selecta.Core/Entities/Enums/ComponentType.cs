namespace Selecta.Core.Entities.Enums;

/// <summary>Corresponde ao enum "TipoComponente" original — valores numéricos preservados.</summary>
public enum ComponentType
{
    Tag = 1,
    DynamicContent = 2,
    StaticContent = 3,
    Header = 4,
    Footer = 5,
}
